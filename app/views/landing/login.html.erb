<!DOCTYPE html>
<html>
    <head>
        <title>Login and registration</title>
    </head>

    <body>
        
        <div class="jumbotron centered-vertical-axis"> 
            <div class="container">

                <h1> Login </h1>

                <form action="/sessions/create" method="POST">
                    <input type="hidden" name="authenticity_token" value="<%=form_authenticity_token%>">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="username" class="form-control" name="login[username]" id="username" placeholder="Enter username">
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" class="form-control" name="login[password]" id="password" placeholder="Enter password">
                        <small id="passwordHelp" class="form-text text-muted">Password are encrypted and safely stored.</small>
                    </div>
                    <button type="submit" name="submit" class="btn btn-primary">Submit</button>
                </form>

                <div class="login-notice">
                    <% if flash[:login_notice] %>
                        <small id="login-notice-message" class="form-text text-warning"> <%=flash[:login_notice]%> </small>
                    <%end%>
                </div>    

            </div>
        </div>

    </body>
</html>